const { cartel } = require('../../base/Clients/Distributors.Clients');
const { Mongoose } = require('../../database/Global.MongoDB.Driver');
const client = global.client = new cartel();
const { GUILD } = require('../../base/Reference/Settings');
const { Collection } = require('discord.js');


// Client Ayarları (Başlangıç)
client.botİsmi = "FIREWALL_DİSTRİBUTORS"

// Client Ayarları (SON)

Mongoose.Connect()
GUILD.fetch(sistem.SUNUCU.GUILD)
client.fetchCommands()
client.fetchEvents()
client.connect(sistem.TOKENLER.AUXLİARY)
